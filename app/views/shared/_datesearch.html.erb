<div class="row">
  <div class="col s5 offset-s1">
    <div class="input-field short-row">
      <select id="operation-year-selector" class="admins-select browser-default" name="query[year]">
        <option value="" selected>Todos</option>
        <% @myYears.each do |year|%>
          <option value="<%= year %>"><%= year %></option>
        <% end %>
      </select>
    </div>
    <div class="label-row">
      <label class="white-text" for="operation-year-selector">AÑO</label>
    </div>
  </div>
  <div class="col s5 offset-s1">
    <div class="input-field short-row">
      <select id="operation-month-selector" disabled class="admins-select browser-default" name="query[month]">
      </select>
    </div>
    <div class="label-row">
      <label class="white-text" for="operation-month-selector">MES</label>
    </div>
  </div>
</div>